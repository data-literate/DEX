# Stage environment patch — production-like resources, info logging
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dex
spec:
  template:
    spec:
      containers:
      - name: dex
        env:
        - name: LOG_LEVEL
          value: "info"           # Standard production verbosity
        - name: DEX_AUTH_ENABLED
          value: "true"
        - name: DEX_RATE_LIMIT_ENABLED
          value: "true"
        resources:
          requests:
            cpu: 100m             # Mirrors production baseline
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
