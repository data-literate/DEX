meta {
  name: List Data Sources
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/data/sources
  body: none
  auth: none
}

params:query {
  ~cursor:
  ~limit: 20
}

docs {
  Returns the list of configured data sources with their type
  (rest_api / scraper) and ingestion schedule.

  Query parameters (all optional):
    cursor — opaque pagination cursor
    limit  — page size (default 20)
}
