meta {
  name: Ready (Readiness)
  type: http
  seq: 2
}

get {
  url: {{base_url}}/ready
  body: none
  auth: none
}

docs {
  Readiness probe — returns {"status": "ready"} when the service
  can accept traffic. Used by Kubernetes readiness probes.
}
